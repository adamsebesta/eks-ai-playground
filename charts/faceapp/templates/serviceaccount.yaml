# Name must match exactly what Terraform's aws_eks_pod_identity_association
# expects (namespace: faceapp, service_account: faceapp) — that pairing is
# the entire trust boundary, nothing in this object itself grants anything.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: faceapp
  namespace: {{ .Release.Namespace }}
