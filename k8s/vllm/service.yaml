apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: inference
spec:
  selector:
    app: vllm
  ports:
    - port: 8000
      targetPort: 8000
# Week 4+ exercise: replace port-forward with an ALB via the
# AWS Load Balancer Controller (Ingress + IngressClass alb).
